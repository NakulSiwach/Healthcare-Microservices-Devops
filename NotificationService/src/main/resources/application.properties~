spring.application.name=NotificationService

# Server port for the service
server.port=8085

# Database configuration (if required)
#spring.datasource.url=jdbc:mysql://localhost:3306/notificationdb?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
#spring.datasource.username=root
#spring.datasource.password=12345
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.show_sql=true

# Eureka client configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
  # Register this service with Eureka
eureka.client.fetch-registry=true
  # Fetch the registry from Eureka to discover other services

#eureka.instance.hostname=localhost

# To disable fetching the registry if you only want registration:
# eureka.client.fetch-registry=false
